# Core extraction
pymupdf
pdf2image
pytesseract
pillow
unidecode
pythainlp
pandas
openpyxl
xlrd
langdetect

# Embeddings / vector store
chromadb
sentence-transformers
python-dotenv

# Graph (optional; used for curriculum hybrid graph RAG)
neo4j

# Optional HTTP embedding API
requests

# Optional: LangChain splitters (CHUNK_STRATEGY=langchain_recursive)
langchain-core
langchain-text-splitters

# SQLite FTS is built-in; ensure Python >=3.11 preferred
